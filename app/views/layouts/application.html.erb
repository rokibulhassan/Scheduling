<!DOCTYPE html>
<html>
<head>
  <title>Scheduling</title>
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'vendor/modernizr', 'data-turbolinks-track' => true %>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <%= csrf_meta_tags %>
</head>
<body>

<nav class="top-bar" data-topbar>
  <ul class="title-area">
    <li class="name">
      <h1><a href="/">Scheduling</a></h1>
    </li>
    <li class="toggle-topbar menu-icon"><a href="#"><span>Menu</span></a></li>
  </ul>

  <section class="top-bar-section">
    <ul class="right">
      <li class="has-dropdown">
        <% if user_signed_in? %>
            <a href="#" data-dropdown="dropdown1"><%= current_user.name %></a>
            <ul id="dropdown1" class="f-dropdown" data-dropdown-content>
              <li><%= link_to "My Profile", edit_user_registration_path(current_user) %></li>
              <li><%= link_to "Log Out", destroy_user_session_path, :method => :delete %></li>
            </ul>
        <% else %>
            <a href="#" data-dropdown="dropdown2">Sign In/Sign Up</a>
            <ul id="dropdown2" class="f-dropdown" data-dropdown-content>
              <li><%= link_to "Log In", user_session_path %></li>
              <li><%= link_to "Register", new_user_registration_path %></li>
              <li><%= link_to "Via Twitter", omniauth_authorize_path(:user, :twitter) %></li>
            </ul>
        <% end %>
      </li>
    </ul>
  </section>
</nav>

<%= yield %>

</body>
</html>

<script type="application/javascript">
    $(function () {
        $(document).foundation();
    });
</script>